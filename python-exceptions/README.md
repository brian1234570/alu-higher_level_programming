# python-exceptions

This project covers Python exception handling: try/except, raising
exceptions, and handling errors gracefully, as part of the ALU Higher
Level Programming curriculum.

## Task
### 0. Safe list printing
`0-safe_print_list.py` - prints x elements of a list using try/except
instead of len(), and returns the number of elements actually printed.
